<form class="profile-page__info-wrap">
    <span class="profile-page__info-wrap-message {{message.type}}">
        {{message.value}}
    </span>
    {{#each inputInfo}}
    <div class="profile-page__info">
        {{{this}}}
    </div>
    {{/each}}
    {{{submit}}}
</form>
